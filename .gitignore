# ============================================================
# 預設策略：白名單制，不是黑名單制。
# 這個 repo 要公開到 GitHub public，出錯的代價是敏感資料外洩，
# 所以反過來做——先擋掉「所有東西」，再明確一項一項放行要公開的內容，
# 不是「先放行所有東西，再逐一想起什麼要擋」。後者只要漏想一項就洩漏。
# ============================================================

# 第一行擋掉根目錄下的所有檔案與資料夾
/*

# 明確放行：src/（Apps Script 原始碼本身）、docs/（給幹事看的操作說明）、
# tests/（Node.js 回歸測試腳本，不依賴 SpreadsheetApp，可以直接 node 執行；
# 階段 F 起新增，只放測試腳本本身，內容一律用 P1／P_ZERO 這種假名假資料，
# 不含任何真實會友姓名或個人資料，可以安全公開）
# 與根目錄的說明文件要公開
!/src/
!/docs/
!/tests/
!/.gitignore
!/README.md

# tools/：第十八輪批次新增的 commit 前敏感資料掃描 script
# （tools/scan-staged-secrets.js）。呢個 script 必須跟住 repo 一齊走，
# 否則換一部機器 clone 落嚟就冇咗個關卡。
#
# 白名單制之下，新增資料夾預設係擋住嘅——所以呢一行係**特登加**嘅，
# 唔係漏咗。放行嘅理由：script 本身按設計唔含任何真實資料
# （安全清單只列「安全樣式」，反面嘅真實網域／姓名一個都冇寫入），
# 而且 tests/scan_staged_secrets.test.js 有三項自我檢查斷言鎖住呢一點。
#
# ⚠️ 日後如果喺 tools/ 加其他檔案，記住呢個資料夾係**會公開**嘅。
!/tools/

# ------------------------------------------------------------
# 以下明確列出「已知」不可公開的項目，即使前面 /* 已經擋住了，
# 這裡再寫一次是為了讓人一眼看懂「為什麼」，日後有人手動改壞
# 前面的規則時，這裡的清單也是一個防呆提醒。
# ------------------------------------------------------------

# .clasp.json：內含 Google Apps Script 專案的 scriptId（等同專案的
# 存取金鑰之一，任何人拿到這個 ID 都能嘗試操作這個 Apps Script 專案）。
/.clasp.json

# HANDOFF.md：專案的開發紀錄與測試步驟，逐輪測試都是對著真實會友資料
# （真實姓名、PersonID、Requests 內容等）操作，內文大量出現真實會友姓名，
# 不能公開。
/HANDOFF.md

# docs/開發歷程.md：完整逐階段開發紀錄，內含大量測試過程用到的真實資料。
# 2026-08-14 才發現上一輪 redaction 漏掉 6 項（1 個真實 Script ID＋5 個
# 真實會友姓名）才被公開推上 GitHub——這種逐字複雜文件很難保證每次改動
# 都掃描到乾淨，而且對外部讀者沒有實質價值（要核實程式碼看 src/ 就夠，
# 這份是給 Ivan 自己回顧「為什麼當初這樣設計」用的），公開純粹是風險、
# 沒有對應的收穫，所以乾脆不公開，跟 HANDOFF.md 同一類處理。
# docs/幹事操作說明.md 不受影響，繼續公開（內容全部用虛構例子，已驗證乾淨）。
/docs/開發歷程.md

# .claude/：Claude Code 的本機專案設定（權限白名單等個人化設定），
# 屬於本機開發環境設定，不是專案原始碼的一部分，不應該公開。
/.claude/

# ------------------------------------------------------------
# 測試資料 / 暫存檔案慣例排除（目前專案內沒有這些檔案，
# 但公開前如果有人在根目錄新增這類檔案，預設也一併擋住）。
# ------------------------------------------------------------
*.csv
*.tsv
*.xlsx
*.json.bak
*.local.*
*test-data*
*testdata*
/scratch/
/scratchpad/
/tmp/
*.log
.DS_Store
Thumbs.db
